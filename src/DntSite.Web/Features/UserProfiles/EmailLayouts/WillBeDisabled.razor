@using DntSite.Web.Features.UserProfiles.Models
<LayoutView Layout="@typeof(EmailsLayout)">
    با سلام
    <br/>
    اکانت شما در صورت عدم ورود به سایت تا یک ماه آینده، به صورت خودکار غیرفعال خواهد شد
    <br/>
    مراتب جهت اطلاع
    <br/>
    <br/>
    با احترام<br/>
    <a style="direction:ltr" href="@Model.SiteRootUri">@Model.EmailSig</a>
</LayoutView>

@code {

    [Parameter] public WillBeDisabledModel Model { set; get; } = null!;

}
