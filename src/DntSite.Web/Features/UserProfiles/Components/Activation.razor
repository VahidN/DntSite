@attribute [Route(UserProfilesRoutingConstants.ActivationData)]

<DntSitePageTitle Group="مدیریت کاربران" PageTitle="فعال سازی حساب کاربری" CurrentPage="@null"/>

<DntCardFrame>
    <FrameHeader>
        فعال سازی حساب کاربری
    </FrameHeader>
    <FrameBody>
        <DntMessageBox IsVisible="true" Type="_alertType" ShowCloseButton="false">
            @_alertMessage
        </DntMessageBox>
    </FrameBody>
</DntCardFrame>
