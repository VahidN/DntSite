@attribute [Route(UserProfilesRoutingConstants.ActivationData)]

<DntSitePageTitle PageTitle="فعال سازی حساب کاربری"/>

<DntCardFrame>
    <FrameHeader>
        فعال سازی حساب کاربری
    </FrameHeader>
    <FrameBody>
        <DntMessageBox IsVisible="true" Type="_alertType" ShowCloseButton="false">
            @_alertMessage
        </DntMessageBox>
    </FrameBody>
</DntCardFrame>
