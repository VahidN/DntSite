@using DntSite.Web.Features.News.Models
<LayoutView Layout="@typeof(EmailsLayout)">
    با سلام
    <br/>

    <a href="@PostUrl">
        لینک
        جدید
    </a> ذیل در سایت ارسال گردیده است:
    <br/>
    <br/>
    <div align="right" dir="rtl" style="font-family: Tahoma; ">
        <strong>از طرف</strong>: @Model.FriendlyName
        <br/>
        <strong>لینک</strong>: <a href="@Model.Url">@Model.Url</a>
        <br/>
        <strong>عنوان</strong>: @Model.Title
        <br/>
        <strong>توضیحات</strong>: @Model.Body.HtmlRaw()
        <br/>
        <br/>
        <strong>وضعیت:</strong>: @Model.Stat
        <br/>
    </div>
</LayoutView>

@code {

    [Parameter] public DailyLinksToAdminsEmailsModel Model { set; get; } = null!;

    private string PostUrl => Model.SiteRootUri.CombineUrl(NewsRoutingConstants.NewsDetailsBase, escapeRelativeUrl: false).CombineUrl(Model.NewsId.ToString(CultureInfo.InvariantCulture), escapeRelativeUrl: false);

}
